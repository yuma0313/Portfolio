<% if user_signed_in? %>
  <% if post_image.favorited_by?(current_user) %>
    <h5 class="card-text">
      <%= link_to post_image.favorites.count, post_image_favorites_path(post_image), method: :delete, remote: :true, class: "fas fa-heart heart-red" %>
    </h5>
  <% else %>
    <h5 class="card-text">
      <%= link_to post_image.favorites.count, post_image_favorites_path(post_image), method: :post, remote: :true, class: "far fa-heart heart-blue" %>
    </h5>
  <% end %>
<% end %>